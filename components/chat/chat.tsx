import { CHAT_PROFILE_QUERYResult } from "@/sanity.types"

async function Chat(
    { profile }: { profile: CHAT_PROFILE_QUERYResult }
) {
    return (
        <div>
            Chat
        </div>
    )
}

export default Chat
